## 1. Metadata Update

- [x] 1.1 Update `metadata.title` in `src/app/layout.tsx` to `"Brian Pontes"`
- [x] 1.2 Add an `icons` entry to `metadata` in `src/app/layout.tsx` pointing to `/code.png`

## 2. Verification

- [x] 2.1 Run the dev server and confirm the browser tab shows the title "Brian Pontes" and the `code.png` icon
- [x] 2.2 Run `npm run build` to confirm the app still builds successfully
